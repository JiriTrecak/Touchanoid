# Disable all the warnings in the pods
inhibit_all_warnings! 

# Enable modules in the application
use_frameworks! 

platform :osx, '10.12'

# Define main pod integration routine
def main_pod_integration
    
    # -------- Code / Data processing helpers

    # Data storage and parsing
    pod 'Warp'

end
 

# Apply pod integration to 'Touchanoid' target
target 'Touchanoid' do
    main_pod_integration
end
 

